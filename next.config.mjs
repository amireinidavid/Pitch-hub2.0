/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    
    domains: [
      'img.clerk.com',
      'images.clerk.dev',
      // Add any other domains you need for images
    ],
  },
};

export default nextConfig;
